var currentLetter = 0;
var intervalID;
var clickCount = 0; 
console.log = clickCount
waves.onclick = function () {
    if (clickCount == 1) purple.style.animationPlayState = 'running'; {
    }
    if (clickCount == 2) emerald.style.animationPlayState = 'running';{
    }
    if (clickCount == 3) blue.style.animationPlayState = 'running';{
    }
}

// waves.onclick = function() {
//     purple.style.animationPlayState = 'running';
// }
